# Simple Docker Flask Demo

This is a simple demo project to show how to run a basic Flask app in Docker.

## Setup

1. Clone the repository.
2. Install Docker.
3. Build the Docker image:

   ```bash
   docker build -t flask-demo .
